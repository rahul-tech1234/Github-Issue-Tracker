1️⃣ What is the difference between var, let, and const?
Ans: Var-var is old version. it is re-assign able variable. if before declare var console if shows undefined. 

let- it is quite similar var. but there small diffrens.if console before variable initialise is show erron.it is re-assigne able  any time it is changeable. modern js Es6 use let not var

const- it is not re-assignable.and when it is declare this it's value alos have to set. it never change.

 2️⃣ What is the spread operator (...)?
 Ans:Spread oparator sprade each element of array individually.

3️⃣ What is the difference between map(), filter(), and forEach()?
Ans: map()-get the each element of the array and return the new array and all element pass the new array.

filter()-it pass the specific element which element true the condition only this element return in a new array  otherwise not return.

forEach()- it is only do somethis and console or dom some thing do in this array function it's cannot return any thing form this forEach().

4️⃣ What is an arrow function?
Ans: arrow function is shortend of the manual function if code one line it automatically return on the other hand if code multiple is alawys "{}" this baracket ;for example const erroFunc=(a,b)=>{
    const =sum=a+b;
    const avg=sum/2;
    return avg;

}  

5️⃣ What are template literals?

Ans: template literals it is modern way to write html code in js file. `` it is easy to write any  variable inside this  template literals. it is Es6. 